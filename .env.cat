# cat (pre-production). Committed on purpose: public URLs and feature flags only.
# Loaded by `astro build --mode cat`.
PUBLIC_SITE_URL=https://cat.gufo-games.labyrainth.com
PUBLIC_ANALYTICS=off
MINIFY=false
